using System;

namespace exercise_39
{
  class Program
  {
    public static void Main(string[] args)
    {
      Console.WriteLine("enter numbers");
      int [] numbers = {};
      
            // i have no idea how to complete these last 4 exercies for part 1, i tried with only a for loop and while loop but njothing seems to work, not sure if there
            // is just a small error or if im completely lost.

      while(numbers[i] != 0)
      {
        for(int i = 0;i < numbers.Length;i++)
        {
          numbers[i]= Convert.ToInt32(Console.ReadLine());
        }
      }
    }
  }
}
